import React from 'react';
import PublishData from 'components/PublishData';

export default { title: 'Publish Data' };

export const Default = () => (
  <PublishData dataPoints="7,700" subscriberCount="2,355" />
);
